#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - print if the last digit is greater than 5, zero, less than 6
 *
 * Description: using the main function
 * this programming prints "Programming is positive, zero or negative"
 * Return: 0
 */
int main(void)
{
	char n;

	for (n = 'a' ; n <= 'z' ; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
